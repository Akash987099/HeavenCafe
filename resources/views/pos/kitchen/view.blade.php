@extends('pos.layout.app')

@section('content')
    <div class="flex-1 overflow-y-auto bg-slate-50 p-4 md:p-6"><div class="max-w-4xl mx-auto"><div class="flex items-center justify-between mb-6"><div><h1 class="text-2xl font-bold text-slate-800">Kitchen Order</h1><p class="text-sm text-slate-400 mt-1">{{ $order->order_number }}</p></div><a href="{{ route('pos.kitchen.orders') }}" class="h-10 px-4 rounded-xl border border-slate-200 bg-white text-slate-600 text-sm font-semibold flex items-center gap-2"><i class="fas fa-arrow-left"></i> Back</a></div><div class="bg-white rounded-2xl border border-slate-200 shadow-sm overflow-hidden"><div class="p-6 border-b border-slate-200 flex items-center justify-between"><div><p class="text-xs text-slate-400">Order Number</p><p class="text-lg font-bold text-slate-800">{{ $order->order_number }}</p></div><span class="px-3 py-1.5 rounded-lg bg-emerald-100 text-emerald-700 text-xs font-bold">CONFIRMED</span></div><div class="p-6"><h2 class="text-base font-bold text-slate-800 mb-4">Items to prepare</h2><div class="divide-y divide-slate-100">@foreach ($order->details as $item)<div class="py-4 flex items-center justify-between gap-4"><div><p class="font-semibold text-slate-800">{{ $item->product_name }}</p><p class="text-xs text-slate-400 mt-1">Quantity: {{ $item->quantity }}</p></div><span class="w-9 h-9 rounded-lg bg-orange-50 text-orange-600 flex items-center justify-center font-bold">{{ $item->quantity }}</span></div>@endforeach</div></div><div class="px-6 py-4 border-t border-slate-200 bg-slate-50 flex items-center justify-between gap-4"><span class="text-xs text-slate-500">Confirmed at: {{ $order->updated_at?->format('d M Y, h:i A') }}</span>@if ($order->status === 'completed')<form action="{{ route('pos.kitchen.orders.delivered', $order->id) }}" method="POST">@csrf<button type="submit" class="px-4 py-2 rounded-lg bg-[#128C7E] text-white text-sm font-semibold">Mark Delivered</button></form>@endif</div></div></div></div>
@endsection
